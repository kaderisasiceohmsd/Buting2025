import streamlit as st

# Tambahkan CSS gradien ke background
st.markdown("""
    <style>
    [data-testid="stAppViewContainer"] {
        background: linear-gradient(135deg, #A1C2BD, #456882);
    }
    [data-testid="stHeader"] {
        background: rgba(0,0,0,0);
    }
    [data-testid="stSidebar"] {
        background: linear-gradient(180deg, #456882, #A1C2BD);
    }
    </style>
    """,
    unsafe_allow_html=True
)
